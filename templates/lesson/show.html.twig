{% extends 'base.html.twig' %}

{% block title %}{{lesson.name}}/{{course.name}}/Study-On{% endblock %}

{% block body %}
    <h1>{{lesson.name}}</h1>
    <p>Курс <a href="/courses/{{course.id}}">{{course.name}}</a></p>
    <a href="{{ path('lesson_edit', {'id': lesson.id}) }}">Редактировать</a>

    {{ include('lesson/_delete_form.html.twig') }}

    <p>{{ lesson.content }}</p>

    {# <a href="{{ path('lesson_index') }}">back to list</a> #}


{% endblock %}
