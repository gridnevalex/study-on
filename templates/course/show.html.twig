{% extends 'base.html.twig' %}

{% block title %}{{course.name}} / Study-on{% endblock %}

{% block body %}
    <h1>{{course.name}}</h1>
    <p>{{course.description}}</p>
    {% if lessons is not empty %}
    <h3>Уроки</h3>
    <ol>
    {% for lesson in lessons %}
    <li><a href="/lessons/{{lesson.id}}">{{lesson.name}}</a></li>
    {% endfor %}
    </ol>
    {% else %}
    <p>В данном курсе нет ни одного урока. Нажмите <strong>Добавить урок</strong> для добавления нового урока.</p>
    {% endif %}
    <br />
    <hr />
    <a href="{{ path('course_index') }}">К списку курсов</a>

    <a href="{{ path('course_edit', {'id': course.id}) }}">Редактировать курс</a>

    <a href="/lessons/new/?course_id={{course.id}}">Добавить урок</a>

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
