{% extends 'base.html.twig' %}

{% block title %}{{course.name}} / Study-on{% endblock %}

{% block body %}
    <h2>{{course.name}}</h2>
    {{ include('course/_form.html.twig', {'button_label': 'Сохранить'}) }}
    <br />
    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
